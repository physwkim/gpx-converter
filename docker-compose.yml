services:
  gpx-converter:
    build: .
    image: gpx-converter:latest
    container_name: gpx-converter
    ports:
      # host:container — change the host side to publish on a different port.
      - "8080:8080"
    environment:
      PORT: "8080"
    restart: unless-stopped
